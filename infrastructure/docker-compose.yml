version: '3.9'
services:
    prometheus:
        container_name: spring-prometheus
        image: prom/prometheus
        ports:
            - "9090:9090"
        volumes:
            - ./prometheus.yml:/etc/prometheus/prometheus.yml